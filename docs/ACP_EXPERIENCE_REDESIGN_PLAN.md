# ACP 体验重设计计划

> 状态：实现完成，Windows 自动化与隔离浏览器验收已完成。2026-09-05 与用户逐项讨论并确认。
> 分支：`codex/acp-managed-sessions`。P1–P10 已实现；最终集成、界面与性能验证以末尾记录为准。
> 参考：本地 Codeg `3ebdfed1d7c0b71d71880a3d2e0f8e09545feae1`，沿用现有许可证与来源记录。

## 产品定义

**RambleDesk 的 ACP 交互对齐 Codeg，并默认内置 RambleDesk 工作流。**

- 外部智能体会话具有原有的 Ramble 会话页面。
- 内部 ACP 会话具有同样的 Ramble 页面，以及独立的 Agent 会话页面。
- 两种页面是同一业务会话的平级视图，各自由 workspace tab 承载，通过明确入口互相跳转。
- 新建页与 Agent 对话页是同一个 tab 的前后状态。左侧列表记录正式会话，顶部 tab 承载视图。

## 已确认的行为

### 智能体管理

- Agents 以设备上的智能体为主对象，展示安装、版本、认证与接入状态。
- 安装或配置完成即可用于新建会话，移除额外的“使用此智能体”步骤。
- 自定义 ACP 入口通过“添加智能体”进入同一列表；启动参数与环境变量属于详情中的高级设置。
- 目录与连接通过 `AgentConfig.catalog_id` 明确关联；迁移一次保守识别旧配置，未确认项保留自定义身份。运行期不通过命令路径猜测身份。
- 原有 MCP、Skills、原生插件由各智能体的配置机制加载。默认反馈能力由 RambleDesk 的会话运行时提供。
- 外部工具接入仍服务“从终端或其他客户端呼叫 RambleDesk”，保留已有闭环。

### 统一托管反馈

- 正式应用的可执行文件支持无界面 `feedback request/get/recover` 命令；独立 CLI 不是用户前置依赖。
- 通用命令通过 HTTP JSON 调用 local-server 的会话专用入口，不要求智能体支持 MCP 或特定插件 SDK。
- 服务端从运行时签发的凭据确定会话归属；调用方不能选择归属，也不能缺少身份时回退到外部全局入口。
- 保留持久 request_id、幂等、反馈包、投递记录、撤销与原会话恢复。请求返回后结束当前轮次，用户提交后由 RambleDesk 续接；不引入长期 shell 等待或轮询。
- 默认内置工作流说明：何时请用户体验/判断、如何创建请求、结束当前轮次、读取反馈并继续。ACP v1 无统一 system prompt 字段，以应用提供的会话上下文实现，恢复时刷新必要说明。
- 只有实际可执行通用命令并访问会话 API 的启动方式才算反馈就绪。旧 MCP/Pi/dsh 出口不得抢走托管会话反馈。

### 新建会话

- “新建会话”立即打开临时草稿 tab，可选择 Agent、工作目录并输入任务，复用最近选择。
- 选择齐全后自动预连接，取得真实模型、思考强度和模式选项；连接过程不阻塞编辑。
- 首次发送前不出现在正式 session 列表。准备状态可以有内部持久记录和 ACP session ID。
- 首条真实用户消息被接受并持久化时转为正式会话，沿用同一 ACP session、同一 tab，自动命名。
- 引导指令不是用户任务，不计为首次发送，也不参与命名。自动标题失败不阻塞工作，手动改名后保留用户选择。
- 发送前更换 Agent/目录保留草稿并重建准备连接，丢弃旧连接的迟到结果。
- 关闭未发送 tab 回收连接与凭据；有输入内容可保留草稿。已开始会话的关 tab 仍只关闭视图。

### 页面与加载边界

- Ramble 请求详情、Ramble 会话页、Task Preview 恢复聚焦请求的界面，移除内嵌 Agent 对话/配置/恢复控制器。
- 内部 ACP 来源请求只增加“查看 Agent 会话”按钮，基于可信来源 ID 打开或聚焦唯一 Agent tab。
- 保留请求自身的提交、投递异常及删除锁定等必要状态，通过轻量数据提供。
- 浏览请求不得加载 Agent 历史或全部 Agent 配置；打开 Agent tab 才加载相应对话。
- 保留草稿、滚动与展开状态；非活动页面停止重渲染，后台运行继续由后端负责。

### Agent 页面

- 打开正式 Agent tab 时自动连接或恢复原会话，已有连接直接复用；关闭或切换视图不停止运行。连接失败显示重试，不循环启动，不重发任务，也不回退成新的空白会话。
- 顶部保留单行标题与跳转 Ramble 的入口；移除常驻连接/停止/删除操作、反馈续接与反馈请求列表，以及正文的“引用到消息”。权限、恢复与连接异常仍需明确显示。
- 正文与紧凑输入框共享居中宽度，输入器随内容增长；目录、Git 分支（detached 时短提交号）与真实上下文占用放在底部。Git 元数据独立低频读取，不参与流式历史刷新，也不执行 Git 命令。
- 按真实 turn 组织用户消息、工作过程和最终回答，参考 Codeg 的 `CompletedTurnContent`、`TurnStats`、`ComposerContextUsage`。
- 思考、工具调用和中间工作记录归入过程区；运行中默认展开，每轮结束自动收起工作详情，显式手动选择优先并保持。最终回答可见，重内容延迟挂载。
- 当前工作进度、错误、取消和待处理权限不能被折叠隐藏。尊重用户手动展开状态。
- 每轮有复制与真实完成时间，过程区展示工作耗时；保持历史分页、跨分页轮次和滚动锚点正确。
- 输入器承载模型/思考/模式控制。当前仅展示 Agent `usage_update` 上报的上下文 `used/size`；累计输入/输出 token、费用与任务消耗尚未接入，无上报时不显示。

## 分批实施与提交

每批先检查变更范围、运行针对性验证，再提交。跨批共享合同先落地；并行任务按文件所有权隔离。
不提交工作区中用户已有的 lockfile、图片或其他无关改动。

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| P0 | 固化本计划、记录基线与验证边界 | 完成 |
| P1 | 请求的可信 Agent 来源和轻量状态投影 | 实现与本地回归完成 |
| P2 | 独立 Agent workspace tab；请求与 Task Preview 去 Agent 化 | 实现与本地回归完成 |
| P3 | 会话专用 HTTP JSON 反馈入口与共享撤销保护 | 实现与本地回归完成 |
| P4 | 应用内置通用反馈命令与真实可执行文件验证 | 实现与本地回归完成 |
| P5 | ACP 统一启动环境、内置工作流与命令续接；处理旧出口 | 实现与本地回归完成 |
| P6 | 准备会话生命周期、首次发送转正、自动标题 | 实现与本地回归完成 |
| P7 | 新建草稿 tab、预连接与动态选项、清理和恢复 | 实现与本地回归完成 |
| P8 | Agents 单一列表与连接身份，自动可用，自定义入口整合 | 实现与本地回归完成 |
| P9 | Codeg 风格轮次展示、按需渲染与复制/时间 | 实现与本地回归完成 |
| P10 | Agent 上报的上下文统计接入与展示 | 实现与本地回归完成 |
| P11 | 集成验收、性能对照、文档与支持范围更新 | 本地集成与界面验收完成；模型、安装包与性能对照见限制 |

批次可继续拆成多个小提交；独立批次可调整落地顺序。每次提交在下方追加结果与未验证项。

## 当前架构

- Agent 自有执行工具调用 `RAMBLEDESK_COMMAND` 指向的应用程序，使用 `feedback request/get/recover`。共享 `rambledesk-feedback-client` 提供命令解析和 HTTP 客户端；桌面入口在 UI/数据库初始化前分派反馈命令。
- 运行时签发私有 `RAMBLEDESK_FEEDBACK_URL`、`RAMBLEDESK_FEEDBACK_TOKEN` 与托管标记。`/agent-feedback/*` 从凭据绑定本地会话，复用既有 scope 撤销和持久 Request/Package/Delivery；生产 ACP 不注入 MCP server，也不选择 Pi 专用反馈扩展。
- prepare 不发送 prompt。每条真实用户与续接 prompt 前置运行时工作流文本，不写入用户活动，不修改全局 Skills。命令缺失、环境丢失或入口不可达必须报告失败，不能回落到外部会话。
- `prepared → active` 由首条真实用户消息的接受与持久化原子完成；标题来自该任务。未发送的连接在关闭、变更或应用重启时清理，客户端恢复的仅是本地输入。
- `agent-draft` 原位替换为 `agent-session`；业务会话的 Ramble 视图仍保留。请求页/Task Preview 只读可信来源与 `getManagedFeedbackStatus` 轻量状态，不挂载 Agent workspace 或拉取历史。
- turn 展示使用真实 ID 和持久起止标记，历史折叠内容延迟挂载；缺少完整边界不显示耗时。usage 只存在当前 runtime instance，未上报、无有效容量或重启后隐藏，不从历史推算。

## 验收标准

- 两个并发 ACP 会话的请求、反馈、恢复和删除互不影响；缺少/撤销凭据不能创建或读取反馈。
- 同 request_id 重试不重复请求；投递结果不明时不盲目重放。
- 桌面二进制命令不启动 UI、不自行启动第二个后端、不打开业务数据库，stdout/退出码稳定。
- 新建未发送不进列表；第一次发送仅转正一次；快速换 Agent/目录、关闭、重开不会串会话或遗留准备资源。
- Agent tab 和 Ramble tab 可以同时打开，互相跳转不丢草稿，同一 Agent tab 不重复创建。
- 请求页和 Task Preview 不调用 Agent 历史查询；长对话仅按需渲染，工作过程折叠真正减少挂载。
- turn 最终回复、工具更新、完成时间、分页与重连无丢失/重复；无 usage 不显示虚构数值。
- Rust DTO 变更重新生成前端类型；执行相关测试、Svelte 检查、合同检查、模块边界与格式检查。
- 最终执行适当的集成检查并进行可用环境内的 UI QA。实际模型、操作系统和安装包验证分别记录；夹具不冒充真实 Agent 或跨平台验收。

## 基线与验证记录

- 开始时 HEAD：`a462e46`；用户已完整测试此前功能闭环。
- 既有未提交内容：`pnpm-lock.yaml`、`apps/desktop/src/assets/rambledesk-app-icon-b.webp`、`bash.exe.stackdump`、`show.gif`。
- 当前运行环境：Windows。Codeg 源码参考与截图用于 UI 规则；不从源码检查宣称性能收益。
- P0：本计划记录已确认需求，后续按完成事实更新状态。
- P7 定向回归：草稿生命周期与 workspace 共 15 个文件、106 项测试通过，覆盖迟到 prepare 清理、关闭与首发竞争、确认丢失后查询、同 ID 重试、目录/配置变化、原位转正与只恢复输入；这不是整仓最终测试数量。
- P9 最终定向回归：chat 相关 38 项通过，覆盖 turn 边界、最终回答保留、分页、折叠与延迟挂载。浏览器验收发现并修复默认折叠时点：每轮结束立即收起，用户显式展开仍保留。
- 最终自动化：前端 120 文件 / 724 项通过；Rust workspace（排除 desktop）316 项、desktop 105 项通过；此后新增的旧 Pi 入口兼容测试 1 项及通用命令链路 2 项通过。Pi/dsh 包测试分别 25/28 项通过。真实 Node ACP 预览示例测试 1 项通过。
- `pnpm check` 零错误、零警告；`contracts:check`、`format:check`、`check:rust-size`、`check:terminology`、`release:check`、整仓 Clippy `-D warnings` 和 Web production build 通过。Web bundle 仍有大 chunk 提示，不将这次视图分离描述为已完成整个应用的包体优化。
- 隔离浏览器验收：草稿预连接取得真实模型/思考选项；首次发送同 tab 转正且左侧由 2 增至 3 个会话；关闭另一未发送草稿不增加侧栏记录；最近 Agent/目录复用；Ramble 与 Task Preview 只有来源入口、没有内嵌 Agent 对话；入口正确打开独立 Agent tab；完成后工作详情默认折叠、手动展开、真实时间、复制后粘贴及 `4,608 / 128,000` usage 展示正常；自定义 Agent 保存后进入同一列表且没有额外启用步骤。
- Agent 首次历史窗口由 1000 调整为 100；205 条历史的 `100 + 100 + 5` 分页验证无漏重。已检查按需挂载与页面边界，但未进行同环境性能基准对照，不填写速度或内存改善比例。
- 旧 Pi 插件的外部工具注册由兼容标记抑制；旧 dsh 插件没有环境退出开关，仍可能附加外部工具及旧指引，当前靠运行时工作流明确选择命令，尚须真实模型检查此组合。
- 历史真实 DeepSeek/dsh 探针和 Pi 离线扩展证据属于先前 MCP/Pi 专用通道，不等于本轮统一 command 已完成模型实测。当前未覆盖新路径真实 Agent 模型闭环、Linux/macOS、签名安装包及同环境性能对照。
- Agent 页面精简后续验收（2026-09-05）：前端全套 122 文件 / 750 项通过；随后补充的读取/连接重试区分与聊天 SSR 定向 16 项通过。Git 元数据单测 4 项、managed HTTP API 11 项、Desktop `cargo check`、Svelte、生成契约、格式/模块边界与 Web build 均通过。
- 本次隔离浏览器验收：预先停止的 CLI 会话打开 Agent tab 后自动以原身份连接；无需点击启动即可切换模型和思考强度并发送；目录与分支、真实上下文、复制/完成时间与自动折叠可见；正文与紧凑输入框居中同宽。切换到 Ramble 后返回，模型、思考选项与上下文保持，未新增会话或重发任务。以上是本地 Node ACP 夹具证据，不替代真实 Agent 安装包验收。
