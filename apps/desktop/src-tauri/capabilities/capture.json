{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "IPC capability for capture editing, scrolling capture, and pinned capture windows",
  "windows": ["capture-overlay", "capture-scroll", "capture-pin-*"],
  "permissions": ["core:default"]
}
