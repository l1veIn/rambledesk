{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Minimal IPC capability for the RambleDesk screenshot selection overlay",
  "windows": ["capture-overlay"],
  "permissions": ["core:default"]
}
